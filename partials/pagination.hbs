<nav class="pagination" role="pagination">
    {{#if prev}}
        <a class="newer-posts" href="{{page_url prev}}">&larr; Latest reports</a>
    {{/if}}
    {{#if next}}
        <a class="older-posts" href="{{page_url next}}">Earlier reports &rarr;</a>
    {{/if}}
</nav>
